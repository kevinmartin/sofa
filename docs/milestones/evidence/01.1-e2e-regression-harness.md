# Milestone 01.1 evidence — deterministic E2E regression harness

**Status: In progress.** This report records observed results only. Sofa [PR #1](https://github.com/kevinmartin/sofa/pull/1) is open and unmerged at `bc5623fa9128e58efb391e1cdd6ce69f339bb9e5`; this milestone is the separate stacked [PR #2](https://github.com/kevinmartin/sofa/pull/2). The required App-sourced check cannot be enabled and proved on trunk until Kevin merges 01 and 01.1 is rebased. Sofa currently has no branch protection or ruleset, so an absent hosted result is not yet a merge block. Every evidence-only commit changes the candidate SHA and needs a new exact-head run before it can be claimed as current.

The first automatic disposable coordinator [run 36063384387](https://github.com/kevinmartin/sofa-disposable/actions/runs/36063384387) discovered the candidate but failed while creating its suite workflow tree. `GITHUB_TOKEN` lacked workflow-file authoring permission. The coordinator now has a separate, environment-confined workflow-authoring credential path; the required `SOFA_DISPOSABLE_WORKFLOW_TOKEN` is not yet configured. Under Kevin's disposable-only authorization, an operator used `gh` to create the same owned suite branch and dispatch the test without passing any real model or publisher credential to candidate jobs. The resulting hosted [run 36063701681](https://github.com/kevinmartin/sofa-disposable/actions/runs/36063701681) completed successfully: `candidate / execute`, `candidate / verify`, and `candidate / publish` all passed on the exact candidate SHA above, base `bc5623fa9128e58efb391e1cdd6ce69f339bb9e5`, and disposable base `e3b5ec89c55861444da3db993294f83bf1d8d0d3`. The execute job used the restricted networkless container and fake ACP peer; the publish job simulated a draft PR against a controlled API. This is hosted model-free candidate evidence, **not** a real disposable draft PR or a green sofa gate.

A second [hosted run 36064493665](https://github.com/kevinmartin/sofa-disposable/actions/runs/36064493665) tested retained-candidate recovery from producer run `36063701681`: GitHub skipped `candidate / execute`, then successfully ran `candidate / verify` and `candidate / publish`. Its report keeps the same candidate digest `189391a44c5ff89937c79edc1e00e35710d962d406418430cfeb948450261138`, records `producer_run_id=36063701681`, and advances the fence from generation 1 to 2 while the bundle remains generation 1. The first run lasted 2 minutes 24 seconds (21:47:49–21:50:13 UTC); recovery lasted 1 minute 32 seconds (21:55:53–21:57:25 UTC). Publication was still simulated, and the run did not test a real interruption or real PR recovery.

The upstream GitHub release API independently reports Copilot CLI v1.0.86's `github-copilot-1.0.86-linux-x64.tgz` digest as `sha256:53284019748ac198c3dbcf9ba17f0541c8fcaaee16e6c644eb0e7285cdfd6112`, matching the workflow pin. The exact pinned Node OCI manifest `sha256:b977d0f785d96029d8d4c0790b6bf1c2a4c72e0f26319808e7ba2e9d966a1ac3` was inspected independently and identifies AMD64. The existing trusted [zero-prompt preflight run 36064542951](https://github.com/kevinmartin/sofa-disposable/actions/runs/36064542951) fetched and checked both package archives, then reported `session/new: accepted without a model prompt` for the package entrypoint. Baseline binary, larger temporary filesystem, and Node-user variants all failed at `initialize` with redacted module/shared-library categories; the workflow compares variants and returns success overall, so its job conclusion alone is not the success evidence. No ACP `session/prompt` was sent.

The first exact-current-head [candidate run 36066846790](https://github.com/kevinmartin/sofa-disposable/actions/runs/36066846790) passed execute, verify, and simulated publication for sofa candidate `777f002e542b1cff85e3fe93b6400a5fc42cc394`, PR base `bc5623fa9128e58efb391e1cdd6ce69f339bb9e5`, and disposable base `64d7d6a036fefefe867c9b5d96d2b7a19214e418`. The restricted execute job logged the fixed `Host-only credential boundary and artifact scan passed` result after checking random runner-only environment/file sentinels and scanning its transport/candidate outputs. Its retained report names suite `p2-272c4ea99a91d71e45dbca36`, fake ACP, one prompt, structural zero provider requests, and candidate digest `b468e8c6e8c3096b39582d6fc3d0894f17a69b1b4c244430a41ea90cf22a8926`. An independent scan of the downloaded report files found no privileged credential field names.

The trusted default-branch [observer run 36067143145](https://github.com/kevinmartin/sofa-disposable/actions/runs/36067143145) validated the exact GitHub run/jobs/artifact, recomputed the candidate digest and one-file gofmt result from disposable main, then opened exactly one [draft disposable PR #32](https://github.com/kevinmartin/sofa-disposable/pull/32), head `a98c56c2278136da4c7f7ba02871807fb39ba40d`. Its only diff is `fixture/greeting.go`. It uploaded `sofa-e2e-observed-36067143145-1` with the run, candidate/base SHAs, digest, and real PR identity. The final `sofa / hosted-e2e` status step failed closed because the App credentials are not installed; no green sofa status was posted. A replay [run 36067387920](https://github.com/kevinmartin/sofa-disposable/actions/runs/36067387920) revealed that the observer used the wrong GitHub `head` filter when looking for its existing PR. GitHub returned no match and rejected a duplicate POST with HTTP 422; #32 remained the sole draft with its original head. Merged disposable [PR #33](https://github.com/kevinmartin/sofa-disposable/pull/33) corrected the query and added finite failed-run retries.

For the next exact pair, sofa candidate `82abd0d8800dcd7bc00aaad8e850f314d6f746ab`, PR base `bc5623fa9128e58efb391e1cdd6ce69f339bb9e5`, and disposable base `4c35a8ef401401c3b689e70c28bcd3529cf28845`, [candidate run 36093757470](https://github.com/kevinmartin/sofa-disposable/actions/runs/36093757470) passed execute, verify, and simulated publication. Trusted [observer run 36093994845](https://github.com/kevinmartin/sofa-disposable/actions/runs/36093994845) validated the result and created one [draft fixture PR #35](https://github.com/kevinmartin/sofa-disposable/pull/35), head `219a51cca9091ca4c29da5c971ae086bdd0274b8`. [Replay run 36094069023](https://github.com/kevinmartin/sofa-disposable/actions/runs/36094069023) reused the same PR and head; its redacted observer artifact is byte-for-byte identical to the first, including candidate digest `cae55dabc1decd498617e9a84fbb4d6243e3abb3294dd2fc36cdc5f24e1ba012` and candidate run ID `36093757470`. Both observer runs passed validation and artifact upload, then failed closed solely at App status publication because the App credential is absent. A review of #33 found that an exhausted suite could starve later scheduled PR discovery; disposable [PR #34](https://github.com/kevinmartin/sofa-disposable/pull/34) adds a two-PR regression test and isolates retry exhaustion while preserving a visible aggregate failure.

After #34 merged, a fresh pair used sofa candidate `d84800b1c15906c0fd35061022c2566d55649151` and disposable base `b99cecc75ad361fb3999bd75748827483f95aa87`. [Candidate run 36094386102](https://github.com/kevinmartin/sofa-disposable/actions/runs/36094386102) passed all three fake jobs. Trusted [observer run 36094581769](https://github.com/kevinmartin/sofa-disposable/actions/runs/36094581769) validated it and opened one [draft fixture PR #36](https://github.com/kevinmartin/sofa-disposable/pull/36), head `d9c793a2123798ea5ab4d283b6712fa4185ac8a8`, with digest `bbb5f86388cc5b4411d378c44de9882b592c47a41a7d44a065652d6dbe63990e`. Again only App status publication failed. A subsequent observer audit found that an older successful run could mask a newer failed rerun if API list order changed. Merged disposable [PR #37](https://github.com/kevinmartin/sofa-disposable/pull/37) now selects the newest exact-suite run before accepting its outcome, with failure/cancellation/rerun regression cases. This evidence commit and the disposable observer change require another exact-pair run before calling the current PR head tested.

For sofa head `355422ca0dbe139a1b80a0b152049212bdd88e39`, PR base `bc5623fa9128e58efb391e1cdd6ce69f339bb9e5`, and disposable base `b1d6244493964defcbfd1890e6f698829981ee90`, [candidate run 36120621101](https://github.com/kevinmartin/sofa-disposable/actions/runs/36120621101) passed execute, verify, and simulated publication. Before observation, the operator created the expected result-branch name at the disposable base SHA, without a suite marker. [Trusted run 36120934496](https://github.com/kevinmartin/sofa-disposable/actions/runs/36120934496) rejected it as `existing fixture branch is not the exact suite candidate`; the branch stayed at its original SHA and no PR was created. After removing only that test ref using an exact-head Git lease, [run 36121071330](https://github.com/kevinmartin/sofa-disposable/actions/runs/36121071330) revalidated the same candidate and created exactly one [draft PR #42](https://github.com/kevinmartin/sofa-disposable/pull/42), head `2a07c1defb2b43be683db726c6b2a1687731a046`. Its redacted version-2 observer artifact records candidate run `36120621101`, artifact `10857466092`, a measured 144,000 ms duration, the exact head/base pair, and `owned_resource_cleanup_state=retained_for_replay`. The App status step still failed closed.

For newer sofa head `9e1e6f2b57527bcad95f83c8e055b2540fb45705`, base `bc5623fa9128e58efb391e1cdd6ce69f339bb9e5`, and disposable base `f93499ac02d38ba9b543da60267b4dc50cae9ce2`, [non-Ready run 36121338144](https://github.com/kevinmartin/sofa-disposable/actions/runs/36121338144) and [completed-redelivery run 36121341431](https://github.com/kevinmartin/sofa-disposable/actions/runs/36121341431) passed. GitHub marked `candidate / execute`, `verify`, and `publish` skipped in both; only `assert-denied` ran. Its redacted artifacts record `admission-denied` and `already-completed` respectively, with zero fake prompts, provider requests, publication writes, and write credentials. These test-only inputs cause the scheduler skip and then validate deterministic admission/ledger state; they do not exercise a live private Project transition.

The first candidate completion did **not** wake the trusted scan when the listener lived in the same workflow file. Adding the default-branch workflow name to that same-file trigger still produced no wakeup on [second successful candidate run 36121136100](https://github.com/kevinmartin/sofa-disposable/actions/runs/36121136100). Disposable [PR #43](https://github.com/kevinmartin/sofa-disposable/pull/43) moved the listener into a separate default-branch workflow that calls the trusted coordinator. A further [short candidate run 36121661960](https://github.com/kevinmartin/sofa-disposable/actions/runs/36121661960) caused automatic [workflow_run 36121705663](https://github.com/kevinmartin/sofa-disposable/actions/runs/36121705663): `wake / observe` succeeded and `wake / coordinate` reached the known missing-App fail-closed path. This proves event delivery and live-PR discovery for the separate listener, not automatic successful dispatch without the planned credentials.

A later exact-caller [run 36148930917](https://github.com/kevinmartin/sofa-disposable/actions/runs/36148930917) passed the edit path and caused automatic [wake 36149214686](https://github.com/kevinmartin/sofa-disposable/actions/runs/36149214686). The observer rejected it because the new `assert-denied` job was skipped on edit, making four jobs where its parser expected three. Disposable [PR #45](https://github.com/kevinmartin/sofa-disposable/pull/45) fixed the strict expected-job set. This prevented a false green and requires a fresh exact-pair success run.

For sofa head `78b8fab2a6e3f2f51c7ceb39d2c00b400424371a`, PR base `bc5623fa9128e58efb391e1cdd6ce69f339bb9e5`, and disposable base `7ffd2ed6d4bb927c8b903e5f5aed0b88cd60e5e3`, [run 36149708412](https://github.com/kevinmartin/sofa-disposable/actions/runs/36149708412) completed execute and verify, then failed at the named `before-publication` injection before simulated write. [Recovery run 36150128226](https://github.com/kevinmartin/sofa-disposable/actions/runs/36150128226) skipped execute, downloaded that producer's retained admission and candidate artifacts, reverified, and completed simulated publication. Its redacted report records `producer_run_id=36149708412`, fence generation 2, original bundle generation 1, digest `ebb1b9252c425088b1d503a886b860cdc47e2e9c389596109e2aa59e3a5c05b6`, one fake prompt, zero provider requests, and one simulated PR post. This proves the hosted scheduler and retained-artifact path after a real workflow failure; it does not claim a real disposable PR was published by the recovery run.

Disposable [PR #44](https://github.com/kevinmartin/sofa-disposable/pull/44) installed the bounded dry-run-first cleanup workflow. Review found a PR-close race; [PR #46](https://github.com/kevinmartin/sofa-disposable/pull/46) added a recheck before close and restoration on a mismatched close response. Both are merged and covered by Go tests.

For sofa head `44d78788990a6124f543198815ae99f37fa0d5bc` and disposable base `706fbb88a6fc9f119d22666a686c2a5efceb9e06`, [candidate run 36150671478](https://github.com/kevinmartin/sofa-disposable/actions/runs/36150671478) passed execute, verify, and simulated publish, with `assert-denied` skipped as expected. The separate automatic [wakeup 36150935634](https://github.com/kevinmartin/sofa-disposable/actions/runs/36150935634) validated the four-job shape and opened exactly one [draft fixture PR #47](https://github.com/kevinmartin/sofa-disposable/pull/47), changing only `fixture/greeting.go`. Its version-2 report artifact `10871128143` records candidate artifact `10871786742`, digest `fbd757b2c8ca3172cbcc3c5a933fa8e0984be2c9a60158c9fbd1cfc5e245e2ef`, 140,000 ms duration, and `retained_for_replay`. The status step failed closed solely because the App is absent; the coordinator also cannot create/dispatch a new suite without the App/workflow-authoring credential. This evidence commit changes the sofa candidate head, so another exact-pair run is required.

The older, superseded suite for sofa head `d84800b1c15906c0fd35061022c2566d55649151` was checked by [cleanup dry run 36151288075](https://github.com/kevinmartin/sofa-disposable/actions/runs/36151288075) with its exact caller/result heads, digest, disposable base, and draft PR #36. [Apply run 36151423789](https://github.com/kevinmartin/sofa-disposable/actions/runs/36151423789) succeeded with the same inputs. Independent GitHub reads found [PR #36](https://github.com/kevinmartin/sofa-disposable/pull/36) closed, both `sofa-e2e/p2-2e07467727a9ff7302779ffc` and `sofa-e2e-result/p2-2e07467727a9ff7302779ffc` absent (404), and current PR #47 still open at unchanged head `2a235502346e3a3b673d0b0af5977b384d348c4c`.

The latest exact-head edit-only [candidate run 36153540691](https://github.com/kevinmartin/sofa-disposable/actions/runs/36153540691) used sofa head `c4b39d8bf0d4019d51ec34a8d26080302f99ca64` and disposable base `706fbb88a6fc9f119d22666a686c2a5efceb9e06`. Its automatic [observer wake 36153825903](https://github.com/kevinmartin/sofa-disposable/actions/runs/36153825903) validated the run and opened [draft PR #49](https://github.com/kevinmartin/sofa-disposable/pull/49). The gate audit then found that this automatic run did not include the denial scenarios: the earlier denial runs were manual and could not block an edit-only green. Merged disposable [PR #50](https://github.com/kevinmartin/sofa-disposable/pull/50) added non-Ready and completed-redelivery calls to the same exact-revision dispatch and made the trusted observer require all 12 job outcomes and both bound denial artifacts. A first multi-scenario [run 36181361693](https://github.com/kevinmartin/sofa-disposable/actions/runs/36181361693) passed the edit and denial jobs, but [observer wake 36181588264](https://github.com/kevinmartin/sofa-disposable/actions/runs/36181588264) correctly refused to publish because its strict ZIP validator expected the denial report under the wrong path. Merged disposable [PR #51](https://github.com/kevinmartin/sofa-disposable/pull/51) corrected the path to GitHub's observed single-file archive layout.

The corrected exact-head [run 36182174987](https://github.com/kevinmartin/sofa-disposable/actions/runs/36182174987) passed all 12 jobs for the same sofa head/base and disposable base `4650127ad357ba34cc95f497a1aa7750e89579f6`. The automatic [observer wake 36182425239](https://github.com/kevinmartin/sofa-disposable/actions/runs/36182425239) validated edit, non-Ready, and completed-redelivery artifacts and opened exactly one [draft disposable PR #52](https://github.com/kevinmartin/sofa-disposable/pull/52), head `07ca4f1f65c31a8293a02a425635651dab8e24fd`, changing only `fixture/greeting.go`. Its redacted version-3 observer artifact records suite `p2-8f4a96f9b4282cd85055753e`, candidate digest `eb6b4d02bcc9fa65f3057663e30662aaf914a1e69f5dd8691a05caf3cc9b57f1`, report artifact `10884214188`, denial artifacts `10884438296` and `10884872316`, 147,000 ms duration, and retained cleanup state. The App status step failed closed: GitHub returned HTTP 404 for the App installation on `sofa`. A separate trusted coordinator [run 36181987372](https://github.com/kevinmartin/sofa-disposable/actions/runs/36181987372) failed at the same lookup. The three environment secret names now exist in `sofa-disposable`, but an App installation granting the configured App access to `sofa` is still required. This evidence commit changes the candidate head, so these runs will be historical rather than current-head gate proof.

The hosted fake runs execute `.github/workflows/e2e-fake.yml`, a secretless surrogate for the shared `execute → verify → publish` scheduler and artifact path; they do **not** run `.github/workflows/work.yml`. A parsed parity check pins the shared dependency, skip/recovery, and candidate/evidence handoffs. Production additionally has `reconcile.yml` admission and `finalize-failure`, uses different artifact names, grants Copilot permission to execution, and performs credential-bearing publication. The fake synthesizes admission in its execute job, runs a networkless fake peer, and simulates publication. Production graph behavior is therefore supported by static contracts and earlier production observations, not proved by these hosted fake runs.

## Trust and resource inventory

- Candidate repository: `kevinmartin/sofa`. Hosted fixture repository: [kevinmartin/sofa-disposable](https://github.com/kevinmartin/sofa-disposable), repository ID `1382055823`, public, default branch `main`. The designated [private Project #2](https://github.com/users/kevinmartin/projects/2) has node ID `PVT_kwHOAAfL7c4Bke1I` and a `Ready` option, as recorded in [milestone 01 evidence](01-delivery-slice.md). Re-read Project identity and credential references before dispatch.
- The normal consumer needs Project and publisher credentials in trusted jobs. The candidate test path must carry no real model, Project, publisher, or sofa-status credential. Only a trusted disposable-default-branch coordinator may own issue/Project setup, real disposable PR publication, and an App-backed sofa status. Candidate results are data to validate, not instructions to execute.
- Every result must bind the exact sofa PR number, head SHA, base SHA, suite ID, disposable run ID, candidate bundle digest, and fixture side effect. A stale or absent result remains non-green. Candidate jobs may simulate GitHub writes against a controlled API; only the trusted fixture may perform real writes after validating artifacts.
- Hosted limits: at most eight dispatches and 45 minutes per candidate. The stable disposable repository is reused. A failed suite retains owned resources for diagnosis and uses a separate bounded cleanup path.

## Historical scenario inventory

| Scenario | Prior evidence or new 01.1 check | Remaining proof |
| --- | --- | --- |
| Numeric caller/reusable input and exact toolkit SHA | `internal/workflow/contracts_test.go` parses and mutates the caller/reusable workflow contract | Hosted dispatch must reach admission with the exact candidate SHA. |
| AMD64 image, Copilot package digest, package entrypoint | 01 recorded the wrong-architecture and standalone-entry failures; static architecture contract now detects drift | Independently download/check package bytes and OCI platform on the hosted runner, then zero-prompt ACP startup. |
| Ready, issue revisions, duplicate/completed delivery | 01 has local admission tests; 01.1 adds explicit local zero-side-effect counters and hosted denial/redelivery skip artifacts | A live private-Project transition still needs the configured coordinator credential. |
| Cancellation, checkpoint provenance, artifact loss, limits | 01 has state and retained-artifact tests plus hosted cancellation/recovery | Named 01.1 local faults and hosted retained-candidate recovery with producer/run identity. |
| Recovered publication after execution skips | 01 hosted recovery opened disposable PR #29 after a workflow fix; parsed condition test exists | New hosted fake-ACP recovery must exercise the candidate job graph. |
| Existing branch and ambiguous PR response | 01 has local lost-response evidence; 01.1 hosted run 36120934496 preserved an unexpected result branch and created no PR | Retained-candidate recovery after an actual controlled interruption remains open. |
| Privileged credential isolation | 01 hosted inert-sentinel run 35984391684 | Current candidate workflow/job and artifact/log sentinel checks, fake profile cannot be selected by issue/config. |

The named local fault checks are distributed across the actual production boundaries. `TestAdmissionCrashAndDedup` preserves a pending dispatch after a crash. `TestRecoveryRequiresTerminalOwnerAndFencesOldRun`, `TestCheckpointRecoveryExpiryAndProvenance`, and `TestDiscardUnavailableCheckpointPreservesBudgetAndPublicationGuard` cover cancellation proof, stale owners, retained/expired/lost artifacts, and cumulative limits. `TestPublishDraftRecoversWhenPRResponseAndFirstLookupAreLost` and `TestPublishDraftStopsWhenHumanChangesCandidateBranch` cover ambiguous publication and unexpected branch heads. `TestFailedVerificationCannotPublish` now joins a real fake-ACP candidate, failing configured check, absent evidence, blocked ledger, and zero publisher writes. `TestCancelledTurnCannotPublishOrResetBudget` joins a cancelled in-flight runner, retained prompt charge, fenced recovery, and zero publication; `TestCancellationKillsRunningAgent` separately proves that cancellation kills the actual ACP process and its descendant. `TestDeliveryBoundaryMatrix` covers the successful path and lost publication acknowledgement. Hosted run 36150128226 proves retained-artifact recovery after a controlled failure, while the hosted adapter still does not run the literal production workflow.

## Acceptance ledger

| ID | Current evidence | Status |
| --- | --- | --- |
| 01.1-A | `TestDeliveryBoundaryMatrix` in `cmd/sofa/e2e_test.go` drove a real fake-ACP stdio edit through `worker.Execute`, `sofa verify`, and `github.PublishDraft` with a fake GitHub transport. It asserted one prompt, one candidate/check digest, one simulated draft PR, and no duplicate write on replay. Hosted run 36063701681 passed execute/verify/simulated publication in the restricted networkless worker. Zero provider requests is supported by the fake peer's lack of a provider client and networkless worker, not a measured external-provider counter. | Local and hosted fake pass |
| 01.1-B | Expanded `cmd/sofa/e2e_test.go` asserts zero ledger attempts/dispatch/prompt/write counts for denied inputs, real publication-guard rejection for late Ready/spec/base changes, and zero agent runner calls for an invalid exact recipe. The initial dispatch counter is a local orchestration sentinel, not an injected GitHub transport. Hosted runs 36121338144 and 36121341431 confirm the relevant candidate jobs skip and report zero prompt/write counts for non-Ready and completed redelivery. | Local and hosted test-only pass; live Project transition pending |
| 01.1-C | The named fault audit maps admission crash, ACP cancellation, accepted-checkpoint loss/expiry/provenance, verification failure, ambiguous publication response/lookup, and branch conflict to local tests. New cross-stage tests prove verification failure blocks publication and cancelled prompts retain budget after owner recovery. Hosted run 36149708412 injected a pre-publication failure and 36150128226 recovered its retained candidate; 36120934496 preserved an unexpected branch. | Local named faults and hosted surrogate samples pass |
| 01.1-D | Parsed and deliberately mutated contracts catch caller numeric conversion, toolkit SHA disagreement, architecture, permission, artifact, and skipped-execution drift. The parity check now requires exact normalized execute, verify, publish, denial, and production failure-finalizer conditions plus both finalizer dependencies; added silent-skip clauses fail locally. The fake jobs' permissions are exact allowlists, so an unlisted write scope also fails locally. Hosted runs 36063701681 and 36150128226 prove normal and skipped-execution scheduling for the fake surrogate only. Upstream release metadata confirms package digest; independent OCI manifest inspection confirms AMD64. | Static contracts and fake scheduler pass; hosted production graph proof pending |
| 01.1-E | Hosted runs 36066846790, 36093757470, and 36094386102 each passed the restricted-container fake edit, verification, and simulated publication on their recorded exact pairs. Trusted observer runs independently validated them and opened one fixture draft per pair (#32, #35, #36), each with only `fixture/greeting.go` changed. Later sofa or disposable commits require a new exact-pair run. | Hosted pass for recorded pairs |
| 01.1-F | Hosted run 36149708412 failed at the controlled pre-publication fault; recovery run 36150128226 skipped execute, reverified retained generation-1 evidence under generation-2 fence, and reached simulated publication. Runs 36120934496 and 36121071330 proved an unexpected result head is preserved with no PR, followed by one PR after exact-ref removal. A real draft PR from the recovered failure path is not claimed. | Hosted surrogate fault/recovery and branch conflict pass; production/trusted path pending |
| 01.1-G | Run 36066846790 passed host-only inert environment/file checks inside the networkless worker and scanned candidate transport files before upload. The downloaded report had no privileged credential field names. Parsed contract mutations enforce read-only candidate permissions and absence of trusted secret mappings. The run tested inert values, not actual secret exfiltration resistance against every possible encoding or future workflow edit. | Hosted inert-sentinel pass |
| 01.1-H | Trusted hosted run 36064542951 checked the pinned package and Node manifest, then accepted ACP `initialize` and `session/new` through `package/index.js` without a model prompt. The workflow's overall green is not itself proof because failed comparison variants are tolerated; the redacted per-variant log identifies the successful package entrypoint. | Hosted zero-prompt pass |
| 01.1-I | `cmd/e2e-fixture` emits redacted candidate and denial reports. The trusted version-3 artifact from observer run 36182425239 includes exact sofa head/base, disposable base, candidate run and artifact links, both denial decisions and artifact links, measured duration, one draft PR #52 identity, and retained cleanup state. Earlier runs 36093994845 and 36094069023 proved byte-identical replay records for one draft. Merged disposable PRs #44/#46 provided bounded cleanup; dry run 36151288075 and apply run 36151423789 closed superseded PR #36 and removed only exact suite refs. | Hosted report and cleanup pass for recorded pairs; final exact-head run pending |
| 01.1-J | The scheduled/default-branch coordinator discovered PR #2 and the trusted observer produced real results. The automatic `workflow_run` wake 36182425239 validated a three-scenario candidate and created draft PR #52. The three gate secret names are present in the disposable coordinator environment. Coordinator and App status publication both received HTTP 404 at the configured App's `sofa` installation lookup, with no personal-token green fallback. App installation access to `sofa`, a current-head pilot status, the App-sourced required rule, and stale-head/base merge-block proof remain pending; parent 01 is unmerged. | Partial; App installation and parent merge pending |

## Handoff and recovery

Use the exact PR head/base and a fresh suite ID when rerunning; never reuse an earlier green status after either SHA changes. Keep candidate jobs secretless and leave sofa PR merge to Kevin. When hosted acceptance becomes available, add run and PR links, redacted report IDs, measured fake prompt/provider-request counts, cleanup state, runner/package/image versions, and the observed required-check behavior here. An unavailable App or coordinator credential must leave the gate pending rather than move credentials into a PR job.

The local integration command `GOCACHE=/private/tmp/sofa-go-cache go test -count=1 ./...` and `go vet ./...` passed on 2026-09-25, and the fast PR check passed for the prior sofa head. These checks do not establish App source attribution or the required trunk rule. [Gate setup instructions](../01.1-gate-setup.md) name the minimal App and environment secret references without requesting values.

# Milestone 01 evidence — safe hosted delivery slice

**Status: Milestone 01 acceptance demonstrated; `sofa` PR remains draft and unmerged.** Kevin's Ready move for issue #6 admitted one Copilot ACP turn, secretless `go-test` passed, and the trusted publisher opened draft PR #7. A later cancellation after admission recovered into a zero-prompt recipe candidate and one draft PR. A separate publication failure recovered from its retained candidate and opened one draft PR without a second agent run. The hosted branch guard rejected unexpected existing branches without overwriting them.

## Implementation and boundaries

- Module: `github.com/kevinmartin/sofa`; target toolkit repository: `kevinmartin/sofa`; target consumer: `kevinmartin/sofa-disposable`. Source and candidate repository identities remain distinct.
- Schema v1: strict `.sofa.yml`, internally computed canonical specification digest and private Project Ready grant, orphan `sofa-state` ledger/spec snapshots, bounded candidate bundle, exact-digest check evidence, and publication intent. The grant binds the Project item, Ready option, and status revision. Issue edits at or after the Ready revision are denied; later content or revision changes fail revalidation. Opaque issue IDs are hashed into portable specification paths.
- The ledger records the owner run attempt, fence generation, cumulative budgets, checkpoint references, observations, and draft publication identity. Failure observations include the fence generation in their identity, so recovery records each failed run without rewriting the prior outcome. Concurrent equivalent observations reconcile to one record after the ledger's compare-and-swap race. The GitHub-backed state store uses non-forced ref updates and retains immutable specification snapshots.
- The worker receives only its Copilot credential and a disposable consumer checkout. ACP file callbacks are scoped to approved files, but the harness's native tools can inspect and edit that checkout; the trusted candidate validator limits what can leave it. The verifier applies candidate data in a separate checkout. The publisher accepts an exact candidate and check evidence, checks current authority/ownership before writes, and uses Git data and PR APIs without executing candidate code.
- Reusable `reconcile.yml` and `work.yml` jobs separate admission, contained execution, secretless verification, publication, and failure finalization. The prototype has one Copilot ACP adapter and one exact `gofmt` recipe. It has no discovery worker, broad board scan, System One router, specialist gates, memory search, watches, or automatic merge.

## Acceptance evidence

| ID | Result | Limit or follow-up |
| --- | --- | --- |
| 01-A | Passed live for issue #6 in run 35957659710: one Copilot ACP candidate, passing `go-test`, draft PR #7 | Record any further version/cost data only if exposed; internal model-call count remains unknown. |
| 01-B | Local admission and concurrent-claim tests reject foreign repositories, ambiguous Project association, malformed input, changed scope, and duplicates; hosted nonexistent, Todo, and edited-after-Ready issues were denied before work; completed issues #6, #12, #19, #22, and #28 were suppressed on redelivery | Provider-side internal model-call telemetry is unavailable; zero inference for skipped work is established by job gating and the recipe bypass path. |
| 01-C | Local admission/checkpoint/publication crash matrix and CAS tests pass, including lost PR response and first lookup; hosted issue #22 was cancelled after admission and recovered into draft PR #24; issue #28 recovered after guarded publication failure, skipped execution, reverified its retained candidate, and opened draft PR #29 within the original retry limit | The hosted cancellation happened before any execution step, so in-flight Copilot termination is covered by fake-peer process tests rather than that cancellation. |
| 01-D | Local stale-fence, forged-tree, and post-reference human branch-update checks pass; hosted issue #23's pre-existing branch remained unchanged and no PR opened after the publisher rejected it | A live race is enforceable at the next state/branch check, not retroactively after a completed write. |
| 01-E | Hosted inert-sentinel boundary canary passed in run 35984391684; targeted adversarial bundle, symlink, hook, evidence, and credential-guard tests passed on Linux | The separate canary checks an equivalent container mapping and pinned source, not every future production edit. Review the production workflow's credential mapping before release. |
| 01-F | Local and hosted Linux fake-peer tests prove timeout/cancellation kills agent descendants, structured auth blocks, and structured quota defers; state tests prove cumulative retries persist and unavailable/expired checkpoints cannot imply success; hosted issue #22 recovery preserved budgets | Real Copilot quota behavior was not induced; provider-specific undocumented errors remain an integration limit. The hosted cancellation happened before Copilot startup, so live Copilot-process termination is not claimed. |
| 01-G | Passed hosted: exact `gofmt` recipe produced a passing one-file candidate and draft PR #13 with `used_agent=false` and zero ACP prompt requests; redelivery of both completed issue #6 and recipe issue #12 skipped work | Provider-side call count is unavailable; zero inference is established by the recipe bypass path, execution artifact, and skipped work job. |
| 01-H | Versioned ledger, execution, candidate, and check evidence; hosted artifacts were inspected; the inert-sentinel security canary passed; command-level operator installation, inspection, and recovery steps are in `examples/consumer/README.md` | Secret non-disclosure is tested for the current boundary and redacted output paths; it cannot guarantee all future workflow edits. |

## Local checks

On 2026-09-24, `go test ./...`, `go test -race ./...`, and `go vet ./...` passed, including the Go zero-prompt preflight, package-entry worker assertion, and recovery additions. Linux amd64 static builds of `cmd/acp-preflight` and `cmd/sofa` passed; the revised work workflow YAML parses. The CLI integration test applies a bounded candidate to the disposable fixture, runs its `go test ./...` gate in a secretless process, and confirms the independent test file was not changed. A second CLI integration test runs the exact `gofmt` recipe without a model credential and inspects the emitted execution artifact: `used_agent=false`, `prompt_requests=0`, and `model_calls=null`. It now also verifies a formatted base produces the fixed `candidate-no-change` failure reason and a dirty base produces `base-checkout`, with zero prompt requests in both cases. Worker-level tests cover failed-turn prompt accounting and invalid recipe inputs. The local baseline fixture test intentionally fails only on the two specified greeting cases. A fault test proves that, when GitHub creates a draft PR but loses both its creation response and the first reconciliation lookup, a retry finds the original PR without a second POST. A retained-artifact test checks exact producer-run identity and fails closed on ambiguous API replies; a real disposable Actions artifact confirmed the expected API shape.

## Live resources and configuration

- [Draft sofa PR #1](https://github.com/kevinmartin/sofa/pull/1) contains the successful delivery commit `d7e77dbcdf24e6c3599da0eafbf8db386dd286ef`, failure-diagnostics commit `a1284ee0821b2a73d994f1536d1dfca0f69731ad`, secret-safe ACP counter/prompt commit `02cb9f83c2a6f0068dd3a7c1910f6c78cd0ebaaf`, and recovered-publication workflow fix `970d0ef331a9490e54405a3a7a8674bec76a4846`; it remains unmerged. The disposable consumer's initial fixture commit is `74304f4`. Kevin merged [disposable PR #5](https://github.com/kevinmartin/sofa-disposable/pull/5), pinning the first delivery workflow to `d7e77dbcdf24e6c3599da0eafbf8db386dd286ef`. Under Kevin's later explicit authority for the disposable repository, the agent merged [consumer PR #10](https://github.com/kevinmartin/sofa-disposable/pull/10), pinning both delivery workflows to `a1284ee0821b2a73d994f1536d1dfca0f69731ad`, then merged the independently tested [greeting PR #7](https://github.com/kevinmartin/sofa-disposable/pull/7). [Setup PR #11](https://github.com/kevinmartin/sofa-disposable/pull/11) introduced only formatting drift while preserving passing tests. Consumer `main` at `bd3b0e73ce07ad015a263b837a308a15aa972ab0` was the exact recipe base; it later advanced to `91b7c7d0b91e96647f0bb3b45190f43516f63f3f` for the distinct interior-whitespace test.
- [Disposable issue #1](https://github.com/kevinmartin/sofa-disposable/issues/1) has the exact fixture specification from `examples/consumer/issue-body.md`. Its canonical digest is `cf358e96c2ddc9e03d6b6f7ff2025637507f508617642267e8d9467213205673`; sofa computes and records it internally. Kevin previously posted an [approval comment](https://github.com/kevinmartin/sofa-disposable/issues/1#issuecomment-5803603054), but the Ready-only gate does not require or read it. Its original attempt is bound to the earlier consumer base commit; subsequent workflow-pin commits advanced `main`, so the same issue cannot be published under that attempt without weakening the exact-base guard.
- [Disposable issue #2](https://github.com/kevinmartin/sofa-disposable/issues/2) has the same bounded fixture specification and no approval comment. Kevin moved it from Todo to Ready in the private Project at 22:40:51 UTC, authorizing the second attempt. The agent did not set Ready on its own behalf.
- [Disposable issue #3](https://github.com/kevinmartin/sofa-disposable/issues/3) has the same fixture specification and was added to the private Project after the AMD64 pin advanced the consumer base. Kevin moved it to Ready at 04:13:00 UTC. It has no approval comment or manually supplied digest.
- [Disposable issue #6](https://github.com/kevinmartin/sofa-disposable/issues/6) has the same bounded fixture specification for the package-entry canary. It was added to the private Project as Todo at 04:50:32 UTC. Kevin moved it to Ready at 04:54:03 UTC after the caller update merged. Sofa did not set it Ready or require an approval comment/digest.
- [Disposable Project #2](https://github.com/users/kevinmartin/projects/2), node ID `PVT_kwHOAAfL7c4Bke1I`, is open and private. Its Status field has a `Ready` option. Consumer `.sofa.yml` was installed with that immutable Project ID in commit `7da67198f7e5c609e8c2b5e18a588f90e39c63fe`. The repository secret list confirms both `SOFA_PROJECTS_TOKEN` and `SOFA_PUBLISH_TOKEN` exist, without exposing values. The GitHub CLI is authenticated as `kevinmartin` with Project access when run with network permission.
- The issue's Project item is unarchived and reports Ready with an update at 21:55:32 UTC, but GitHub's GraphQL and REST issue timelines contain no `ProjectV2ItemStatusChangedEvent`. The Project's own `items` connection reports zero items even though the issue's `projectItems` connection and direct item lookup report this item. The status field value's `creator` is `github-project-automation`, which does not identify the actor who performed the later update. At Kevin's request, the revised gate trusts current Ready in this private, access-controlled Project as authority and stores its exact status revision; it does not claim to verify the mover. Project write access and automations are therefore security-relevant configuration.
- Hosted execution used the consumer's ephemeral `GITHUB_TOKEN` for controller/finalizer state writes and a successful Copilot ACP turn. [GitHub documents this Copilot CLI Actions mode](https://docs.github.com/en/copilot/how-tos/copilot-cli/use-copilot-cli-in-actions) with `copilot-requests: write` and no extra model secret; [personally owned repository usage is charged to the owner's Copilot seat](https://docs.github.com/en/copilot/concepts/agents/copilot-cli/copilot-cli-in-github-actions). The separate `SOFA_PUBLISH_TOKEN`, scoped to only `sofa-disposable` with Contents and Pull requests write permission, created the draft branch and PR in the trusted publisher job. The repository default Actions token is read-only. The repository-wide toggle that also allows Actions to approve PR reviews remains disabled. No Project or publisher credential enters the agent container; only the Copilot credential does.
- Finite trial: one Ready issue, one ten-minute agent attempt, one reserved ACP prompt per attempt, zero repair attempts, and one infrastructure retry. Issue #6 used one ACP prompt and produced a candidate, passing check, and draft PR. Copilot's internal model-call count and actual cost remain unknown because ACP did not report them.

## Hosted probes and blockers

- [Run 35925564462](https://github.com/kevinmartin/sofa-disposable/actions/runs/35925564462) failed during reusable input evaluation, before any job or model could start: `gh workflow run -f issue_number=1` supplied a string where `workflow_call` required a number. The consumer caller and template now use `fromJSON(inputs.issue_number)` to convert the CLI's string input to a numeric reusable input; consumer commit `94a14a6c0d70cf58090bf2719fcce5f1da1b92cb` contains the fix. GitHub [documents matching reusable input types](https://docs.github.com/en/actions/how-tos/reuse-automations/reuse-workflows) and [`fromJSON` conversion](https://docs.github.com/en/actions/reference/workflows-and-actions/expressions).
- [Run 35925894056](https://github.com/kevinmartin/sofa-disposable/actions/runs/35925894056) reached the `reconcile / admit` job definition, but GitHub did not start its steps. Its annotation says: “The job was not started because your account is locked due to a billing issue.” The `work` job was skipped. Kevin subsequently resolved this account-side lock.
- [Run 35927951292](https://github.com/kevinmartin/sofa-disposable/actions/runs/35927951292) started the pinned reusable workflow on `ubuntu-24.04` with `Actions: read` and `Contents: write` job token permissions. The controller built and read `SOFA_PROJECTS_TOKEN` and `SOFA_STATE_TOKEN`; its `Admit the designated issue` step returned `sofa: current issue lacks matching owner specification approval and Ready authorization`. The Project token could read enough data to reach authorization; the missing Ready event caused the old rule to deny. The `work` job was skipped, so this run performed no ACP/model work and produced no candidate or PR.
- [Run 35928948739](https://github.com/kevinmartin/sofa-disposable/actions/runs/35928948739) used Ready-only toolkit commit `a535feaaa77dc27d50f9dd3a78cce8c791bdf4a7` and consumer commit `f3e36c19930d2da7f26f61aa5284ee42b54f97a1`. `reconcile / admit` succeeded, proving the single Ready gesture was accepted. `work / execute` failed at the pinned Copilot CLI archive SHA-256 check before starting the agent. The workflow had accidentally used the ARM64 hash for the x64 archive. Its failure-finalizer job succeeded; verify and publish were skipped. GitHub's release API and an independent download both confirm x64 digest `ea4a519d7b2ff54e9c7d10ae9921cf4ab3f5041489ec340852f47f4f63fc535b`; corrected toolkit commit `dd7cd9d360a406666c37c71a303b700755735c72` retains the digest check.
- [Run 35929366631](https://github.com/kevinmartin/sofa-disposable/actions/runs/35929366631) attempted to rerun issue #1 after the consumer workflow pin advanced `main`. Admission rejected `admitted snapshot changed or issue already has another authorization` before work. The original attempt's base is immutable, and publication also requires the default branch to match that base, so a new Ready issue is the safe live path for the updated workflow. Neither run consumed an ACP/model prompt.
- [Run 35929778711](https://github.com/kevinmartin/sofa-disposable/actions/runs/35929778711) admitted issue #2 without an approval comment, fetched and verified Copilot CLI v1.0.86 x64, then failed before starting the agent: the pinned Node 24.21 container manifest was `linux/arm64/v8` on an `ubuntu-24.04` AMD64 runner (`exec format error`). The failure finalizer succeeded; verify and publish were skipped. The official Node OCI index shows its matching `linux/amd64` manifest is `sha256:b977d0f785d96029d8d4c0790b6bf1c2a4c72e0f26319808e7ba2e9d966a1ac3`. Toolkit commit `4361410f8a3a7457db4f9d5348bebd170d8d4145` uses that manifest and explicitly selects `linux/amd64`. The Docker daemon is unavailable locally, so a native execution preflight could not be performed; the manifest mapping was checked independently against the registry.
- [Run 35954758205](https://github.com/kevinmartin/sofa-disposable/actions/runs/35954758205) admitted issue #3, fetched the checked x64 Copilot archive, and started the checked AMD64 Node container. The worker failed after roughly one second with sanitized `agent: protocol failure`; it uploaded an `infrastructure` failure artifact and the failure finalizer succeeded. Verification and publication were skipped. There is no candidate, check evidence, draft PR, or supported claim of a model call. The adapter did not identify the failed phase or expose raw Copilot messages. A local zero-prompt probe using the same v1.0.86 Copilot CLI on macOS accepted ACP `initialize` and returned structured `-32000` for an intentionally invalid token at `session/new`; this does not prove the hosted token's status.
- [Run 35955560146](https://github.com/kevinmartin/sofa-disposable/actions/runs/35955560146) used the trusted, zero-prompt ACP preflight with the same read-only container: the Copilot process exited during `initialize`, before an ACP response. It sent no prompt.
- [Run 35955813638](https://github.com/kevinmartin/sofa-disposable/actions/runs/35955813638) repeated that probe with the baseline runner, a 512 MiB temporary filesystem, and the image's `node` user. All three exited at `initialize` with code 1 and the fixed `module unavailable` category. Changing temporary storage size or user did not fix startup. The diagnostic reports no raw stderr or token and sends no prompt.
- [Run 35956659574](https://github.com/kevinmartin/sofa-disposable/actions/runs/35956659574) dispatched the default-branch canary for verified nonexistent issue #9999. The `reconcile / admit` job failed while reading that issue (`GitHub GraphQL query incomplete or unavailable`); GitHub marked the entire `work` job skipped. No Copilot container or model prompt was started by this run. This is one hosted negative-path observation, not proof of every denial case or provider-side model-call telemetry.
- [Run 35957505387](https://github.com/kevinmartin/sofa-disposable/actions/runs/35957505387) dispatched issue #6 while it was still Todo and before the caller pin merged. Admission returned `current issue lacks an unchanged specification and private Project Ready authorization`; the `work` job was skipped. The run created no attempt or model prompt. This proves the live Project status gate denied this one non-Ready issue; it does not prove every unauthorized case.
- [Run 35957659710](https://github.com/kevinmartin/sofa-disposable/actions/runs/35957659710) used consumer base `66f357dcabfb64405cef04540f0ec570b31110c3`, toolkit `d7e77dbcdf24e6c3599da0eafbf8db386dd286ef`, Copilot CLI v1.0.86's checked package, the checked Node 24 AMD64 image, and the private Project Ready revision at 04:54:03 UTC. Admission (41 seconds), execution (58 seconds), secretless verification (35 seconds), and publication (79 seconds) all succeeded; wall time from run creation to completion was 3 minutes 49 seconds. Execution artifact v1 reports `used_agent=true`, one ACP prompt request, and `model_calls=null` because the provider did not expose internal calls. Candidate digest `e9753c4c54610865f2a54c123335447e2f9ca56cd85857514a87c7945a8053d1` is identical before and after verification. Versioned `go-test` evidence reports `passed=true` for that digest. GitHub retains the manifest, candidate, verified candidate, and check evidence artifacts. [PR #7](https://github.com/kevinmartin/sofa-disposable/pull/7), head `de07e45dd7ee8857e3bf1130fda5f3644e0ff7f7`, changes only `fixture/greeting.go` to trim names and use `Hello, friend` when blank. It opened as a draft, was independently tested, and was later merged under Kevin's explicit disposable-repo authority.
- [Run 35958018508](https://github.com/kevinmartin/sofa-disposable/actions/runs/35958018508) redelivered completed issue #6 against the same caller/base. Admission completed with `dispatch=false`; the entire work job was skipped and no second PR appeared for its `sofa/73f3b7b46c603549be201a61` branch. The skip proves sofa did not issue another prompt in that run; Copilot's internal call count remains unavailable.
- Kevin moved [issue #8](https://github.com/kevinmartin/sofa-disposable/issues/8) to Ready at 05:03:00 UTC for the cancellation canary. [Run 35979692952](https://github.com/kevinmartin/sofa-disposable/actions/runs/35979692952) admitted it. A cancellation was requested after admission while the execution job was starting. GitHub marked the run cancelled, but the worker step had already run for about 23 seconds and returned the fixed `execution input or candidate validation failed` error before cancellation completed. The first failure artifact is `validation` at generation 1, so this does not prove process termination on an in-flight ACP turn.
- [Run 35979921042](https://github.com/kevinmartin/sofa-disposable/actions/runs/35979921042) redelivered the same Ready issue after GitHub confirmed the first run terminal. Admission reclaimed the attempt at generation 3 with no recovery checkpoint or publication intent. The worker again returned `execution input or candidate validation failed`; its failure finalizer succeeded and blocked the attempt as `validation`. The live ledger retained two reserved prompt-call units, zero repairs, one infrastructure retry, and 1,200 reserved runtime seconds, exactly its cumulative limits. No candidate, check evidence, or PR was produced for issue #8. The generic error is consistent with a no-change result after a completed ACP turn, but current sanitized telemetry does not prove that cause. This is evidence of fenced redelivery and preserved budgets, not a successful hosted recovery.
- [Issue #9](https://github.com/kevinmartin/sofa-disposable/issues/9) was moved to Ready after consumer PR #10 merged, on base `10b8d89c226235a283fcdcc225eae65371234721`. [Run 35981274480](https://github.com/kevinmartin/sofa-disposable/actions/runs/35981274480) was cancelled while its isolated worker step was active. The worker completed with execution failure v2 reason `candidate-no-change`, `used_agent=true`, and one ACP prompt before cancellation terminated the job; thus it still does not prove in-flight process termination. [Run 35981586827](https://github.com/kevinmartin/sofa-disposable/actions/runs/35981586827) reclaimed the same attempt at generation 3 and again returned `candidate-no-change`; the failure finalizer blocked it. The ledger retained two reserved prompt units, one infrastructure retry, 1,200 reserved runtime seconds, and no checkpoint or publication. No candidate, check evidence, or PR was produced for issue #9.
- [Issue #12](https://github.com/kevinmartin/sofa-disposable/issues/12) requests only the configured exact `gofmt` recipe and contains the marker `<!-- sofa:recipe=gofmt -->`. The agent added it to the private Project and moved it to Ready at 09:35:02 UTC after verifying base `bd3b0e73ce07ad015a263b837a308a15aa972ab0` passed `go test ./...` and reported exactly `fixture/greeting.go` from `gofmt -l`. [Run 35982129594](https://github.com/kevinmartin/sofa-disposable/actions/runs/35982129594) passed admission, recipe execution, secretless verification, and publication. Execution artifact v1 reports `used_agent=false`, `prompt_requests=0`, and `model_calls=null`; source code bypasses ACP for this exact recipe. Candidate digest `3080225796671cc628906186b18925630810ab0852a13dea801ffa348de401f8` is identical before and after verification; `go-test` evidence v1 reports `passed=true`. The one-file formatting change opened [PR #13](https://github.com/kevinmartin/sofa-disposable/pull/13), head `be377fc4b1a0459022acd261ceb0edae80c4e73d`, originally draft and later merged under Kevin's disposable-repo authority. [Run 35982562110](https://github.com/kevinmartin/sofa-disposable/actions/runs/35982562110) redelivered completed issue #12: admission succeeded with `dispatch=false`, the whole work job was skipped, and no second PR appeared.
- [PR #14](https://github.com/kevinmartin/sofa-disposable/pull/14) added one independent failing assertion, `Greeting("Ada   Lovelace") == "Hello, Ada Lovelace"`, then merged to consumer base `91b7c7d0b91e96647f0bb3b45190f43516f63f3f`. [Issue #15](https://github.com/kevinmartin/sofa-disposable/issues/15) requests this distinct interior-whitespace behavior and was moved to Ready at 09:42:07 UTC. [Run 35982848614](https://github.com/kevinmartin/sofa-disposable/actions/runs/35982848614) was cancelled while the worker step was active, but the worker again returned v2 `candidate-no-change` with one ACP prompt before GitHub stopped the job. It produced no candidate or PR. [Recovery run 35983161871](https://github.com/kevinmartin/sofa-disposable/actions/runs/35983161871) reclaimed the attempt and also returned `candidate-no-change`; its failure finalizer succeeded, with no candidate, verification, or publication. This distinct issue shows that repeated no-change outcomes are not explained solely by the earlier duplicate greeting specification. It does not establish why Copilot chose not to edit; fixed, secret-safe ACP tool and permission counters are being added before changing permissions.
- [Issue #16](https://github.com/kevinmartin/sofa-disposable/issues/16) entered Ready at 09:52:41 UTC, then its body was edited at 09:52:54 UTC. [Run 35983946239](https://github.com/kevinmartin/sofa-disposable/actions/runs/35983946239) rejected it with `current issue lacks an unchanged specification and private Project Ready authorization`; the entire work job was skipped. No ACP prompt, candidate, or PR was started by that run.
- [Security canary PR #17](https://github.com/kevinmartin/sofa-disposable/pull/17) added a standalone `workflow_dispatch` check pinned to toolkit `02cb9f83c2a6f0068dd3a7c1910f6c78cd0ebaaf` and was merged under Kevin's disposable-repo authority. [Run 35984391684](https://github.com/kevinmartin/sofa-disposable/actions/runs/35984391684) passed existing adversarial candidate/path/symlink/hook/evidence/credential tests on the hosted Linux runner. Its Docker probe reproduced the production worker's mount and environment allowlist with randomly generated inert host-only Projects, publisher, state, and App sentinels. Among credential names, the container saw only the inert model-token mapping; it could not see the privileged environment names or host-only sentinel file. It could write the candidate mount but could not write the trusted mounts. The probe scanned mounted paths for the privileged sentinel values without printing them and returned `isolated boundary probe passed`. The job used only read-level Actions permissions and created no candidate or PR. This validates the tested boundary configuration and guards; it does not prove every possible encoding or future workflow edit is safe.
- All five retained files from [recovery run 36011946615](https://github.com/kevinmartin/sofa-disposable/actions/runs/36011946615) were downloaded and scanned without printing their contents; none contained a privileged credential field name (`SOFA_PROJECTS_TOKEN`, `SOFA_PUBLISH_TOKEN`, `SOFA_STATE_TOKEN`, or `SOFA_APP_PRIVATE_KEY`). The security canary above checked randomly generated sentinel **values** in mounted paths at runtime. These checks cover the observed artifact schemas and tested container boundary, not arbitrary future output or every possible exfiltration channel.
- [Consumer PR #18](https://github.com/kevinmartin/sofa-disposable/pull/18) merged the four-line caller pin to toolkit `02cb9f83c2a6f0068dd3a7c1910f6c78cd0ebaaf` at default-branch commit `1b31058a15b7acc38b40cca9412e17fcf4905662`. [Issue #19](https://github.com/kevinmartin/sofa-disposable/issues/19) has a fresh interior-whitespace specification and entered Ready at 10:00:16 UTC. [Run 35984685694](https://github.com/kevinmartin/sofa-disposable/actions/runs/35984685694) passed all four jobs and opened [draft PR #20](https://github.com/kevinmartin/sofa-disposable/pull/20), head `f3c072e36e4c7c28cb55898c4923e44a71e7d11a`. Its execution artifact reports one ACP prompt, 235 update notifications, one permission request with zero denials, two read tool calls, one edit tool call, zero execute calls, zero failed-status updates, and unknown internal model-call count. The sole `fixture/greeting.go` change collapses interior whitespace. Candidate digest `006e8c844344c3ae62d14f30099b187c6cdd2b3e04067dbfad3ab80351cd7679` matches the independently verified bundle; versioned `go-test` evidence passed. This proves the explicit file-edit path works in the current restricted container. It does not reveal which tool choices caused prior no-change turns, because those runs predated the fixed counters.
- [Run 36011315750](https://github.com/kevinmartin/sofa-disposable/actions/runs/36011315750) redelivered completed issue #19 on the unchanged base: admission returned `dispatch=false` and the work job skipped. PR #20 was independently tested with `go test ./...` and merged under Kevin's disposable-repo authority at `378be81ef5288a980fde1c53a7b9b2f12b4a3fff`.
- [Setup PR #21](https://github.com/kevinmartin/sofa-disposable/pull/21) introduced formatting drift only after PR #20 made the fixture pass all tests; the setup itself passed `go test ./...` and `gofmt -l` reported only `fixture/greeting.go`. It merged to base `dcc8024ec0c9d588c2c010ecc892da20707766d9`. [Issue #22](https://github.com/kevinmartin/sofa-disposable/issues/22) is an exact `gofmt` recipe on that base. [Run 36011758177](https://github.com/kevinmartin/sofa-disposable/actions/runs/36011758177) completed admission and uploaded its manifest, then was cancelled before any execution step started. [Redelivery run 36011946615](https://github.com/kevinmartin/sofa-disposable/actions/runs/36011946615) reclaimed generation 3 and completed execution, secretless verification, and publication. The one-file candidate had `used_agent=false`, zero ACP prompts, and digest `ed42e1f421c10fd2221190dc212bcb618f8327e58670783580e49dbd72d270fb`; `go-test` passed and [draft PR #24](https://github.com/kevinmartin/sofa-disposable/pull/24) opened at head `ee03e099dc2bf934422a2277fdd32b94dc9d6f60`. The ledger retains one infrastructure retry and 1,200 reserved runtime seconds across cancellation and recovery, with zero reserved model calls. [Run 36013228044](https://github.com/kevinmartin/sofa-disposable/actions/runs/36013228044) redelivered this now-completed issue: admission succeeded, work skipped, and no duplicate PR opened.
- [Issue #23](https://github.com/kevinmartin/sofa-disposable/issues/23) is a separate exact recipe on the same base, moved to Ready at 14:20:35 UTC. Its deterministic attempt branch is `sofa/fdc4784b05bde76a78e63d12`, independently checked against its real admission manifest. The agent created that branch at the unchanged base and advanced it by a normal, non-force human commit to `7fcec7861af1122f07c77c2789b64dda50aacc0d` before dispatch. [Run 36012592311](https://github.com/kevinmartin/sofa-disposable/actions/runs/36012592311) passed execution and verification, then its publisher failed with the fixed error `existing branch does not match admitted candidate`. The branch still pointed to the human commit and no PR had that head. This demonstrates the next-check guard; a write that completes before a competing human update cannot be undone retroactively.
- [Run 36013237823](https://github.com/kevinmartin/sofa-disposable/actions/runs/36013237823) redelivered issue #23. Admission reused the retained verified candidate, execution skipped, and verification passed again. Publication was unexpectedly skipped by GitHub Actions: the `publish` job's condition lacked an explicit status function, so GitHub's implicit `success()` treated the intentionally skipped upstream `execute` job as a reason to skip publication. The branch remained `7fcec7861af1122f07c77c2789b64dda50aacc0d` with no PR. The workflow now uses `always() && needs.verify.result == 'success'`, with a focused regression test; fresh-base issues #26 and #28 below verified the corrected path live.
- Toolkit commit `970d0ef331a9490e54405a3a7a8674bec76a4846` contains that workflow fix and regression test. [Disposable caller PR #25](https://github.com/kevinmartin/sofa-disposable/pull/25) pinned all four toolkit references to the same commit and was merged under Kevin's disposable authority at base `7703d1b2cac02973afcc4c78ebb3d1e3596f47c5`. [Issue #26](https://github.com/kevinmartin/sofa-disposable/issues/26) was added to the private Project and moved to Ready for a fresh-base publication-recovery retest. Its admitted attempt branch `sofa/2178019f3c26fb71ac647f14` was pre-seeded with a controlled non-force commit, `b3e8220d48be04c9499b5e735ca90e2e2a76685c`, before publication. [Run 36014339994](https://github.com/kevinmartin/sofa-disposable/actions/runs/36014339994) passed admission, zero-agent recipe execution, and secretless verification; publication rejected the pre-existing branch with `existing branch does not match admitted candidate`. The branch SHA was still `b3e8220d48be04c9499b5e735ca90e2e2a76685c` afterward.
- [Redelivery 36014842197](https://github.com/kevinmartin/sofa-disposable/actions/runs/36014842197) did reach publication after execution skipped and secretless re-verification passed. The publisher again rejected the unchanged controlled branch; no PR was opened. After the agent removed only its controlled branch with an exact-SHA lease, [a third dispatch](https://github.com/kevinmartin/sofa-disposable/actions/runs/36015279719) stopped at admission with `persistent attempt limit reached`: the ledger retained the one allowed infrastructure retry. No work job ran. The agent restored the same controlled branch commit. This is a budget-safety observation; fresh issue #28 below demonstrates successful publication on one allowed retry.
- [Disposable PR #27](https://github.com/kevinmartin/sofa-disposable/pull/27) added the fake ACP peer and state packages to the existing Linux/inert-sentinel security canary. [Run 36015502488](https://github.com/kevinmartin/sofa-disposable/actions/runs/36015502488) passed `go test -count=1` on hosted Linux for `internal/agent` and `internal/state`, including timeout/cancellation descendant cleanup, structured auth/quota classification, and persistent retry tests. The inert-sentinel container probe also passed. This run used no Copilot prompt.
- [Issue #28](https://github.com/kevinmartin/sofa-disposable/issues/28) was moved to Ready on consumer base `341720db3d7b6e2aeead025a0546df2b2a5ecbb1` for a one-retry publication recovery check. Its admitted branch `sofa/db187a39c44c93271d00f60b` was pre-seeded with the agent's controlled commit `4bd6357b81c219b1fa756d7ea2323e87ba238772`. [Run 36015615249](https://github.com/kevinmartin/sofa-disposable/actions/runs/36015615249) passed zero-agent recipe execution and verification, then rejected that branch with `existing branch does not match admitted candidate`; the branch SHA was unchanged. After this run became terminal, the agent deleted only its controlled branch with an exact-SHA lease. [Redelivery 36016177132](https://github.com/kevinmartin/sofa-disposable/actions/runs/36016177132) reclaimed the retained verified candidate. Execution skipped, secretless verification passed, and publication opened [draft PR #29](https://github.com/kevinmartin/sofa-disposable/pull/29), head `fb4f6beee261e83345c37f64229f6ec6f9240264`, changing only `fixture/greeting.go`. The ledger is `draft` at generation 3 with zero model calls, one infrastructure retry of one allowed, 600 reserved runtime seconds of 1,200 allowed, and one PR identity. This is hosted successful recovery after accepted checkpoint and failed publication, without a second execution.
- [Run 36016577260](https://github.com/kevinmartin/sofa-disposable/actions/runs/36016577260) redelivered completed issue #28. Admission succeeded with `dispatch=false`; the entire work job skipped, and the existing draft PR #29 remained the sole PR for its attempt branch.
- Kevin merged [disposable diagnostic PR #4](https://github.com/kevinmartin/sofa-disposable/pull/4) at its original head `2aab1c8`, adding the fixed-category zero-prompt comparison to consumer `main` at `9250a81c83d34a2c1330000da69716a443731be3`. [Run 35957061742](https://github.com/kevinmartin/sofa-disposable/actions/runs/35957061742) verified both Copilot v1.0.86 archives by SHA-256. The standalone baseline, larger temporary filesystem, and image `node` user all exited during `initialize` with `module unavailable` and `shared library mapping failed`. The official package entrypoint completed `initialize` and `session/new` with zero stderr bytes and no model prompt. This isolated the packaging/runtime failure that the merged caller PR #5 resolved. Local Docker is installed but its daemon is unavailable.

## Reproduction and handoff

The complete delivery path is live-proven for issue #6, the zero-inference exact-recipe path for issues #12, #22, and #28, post-admission cancellation/recovery for issue #22, and publication-checkpoint recovery for issue #28. Human branch changes were rejected live for issues #23 and #26. Kevin explicitly authorized sofa to operate and merge within `sofa-disposable`; the toolkit's draft PR #1 remains for his review and merge. Reusing an issue after its admitted default-branch base changes would breach the exact-base guard. The admitted manifest artifact is `sofa-manifest-<run>-<attempt>`, the retained candidate is `sofa-verified-candidate-<run>-<attempt>`, and trusted check evidence is `sofa-evidence-<run>-<attempt>`. After a terminal cancellation without changing the consumer base, rerun the same issue; the ledger must reclaim or suppress it based on the recorded phase and fetch only the checkpoint's producing run. If the retained candidate artifact has disappeared before publication intent, admission checks the exact producer run, discards only that checkpoint, and reserves a fresh execution under the original cumulative limits. An API error or a missing artifact after publication intent blocks for investigation of possible branch/PR state. If publication reports an existing branch mismatch, inspect its head and leave it untouched; rerunning does not authorize a force push or replacement PR.

Automatic approval review rejected enabling the repository-wide Actions PR-review approval setting because it grants persistent bot approval capability beyond this draft-PR canary. The safer publisher-token path above leaves that setting disabled.

Automatic approval review also rejected directly publishing the diagnostic workflow to consumer `main`, because default-branch workflow changes were not explicitly authorized outside review. It separately rejected marking diagnostic PR #4 ready, interpreting the draft state as a review boundary. Kevin subsequently reviewed and merged PRs #4 and #5 himself. Sofa's toolkit PR #1 remains draft and unmerged.
